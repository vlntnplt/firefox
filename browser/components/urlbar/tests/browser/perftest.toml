[DEFAULT]
prefs = [
  "browser.ml.enable=true",
  # Contextual embeddings are only selected on mac and Windows; Linux stays on
  # static embeddings regardless of this pref.
  "places.semanticHistory.embeddingType=contextual",
]

["browser_urlbar_semantic_history_perf.js"]
disabled = "Disabled since we want to run this only as perftest, not in regular CI."
